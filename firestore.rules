rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /qrsSessions/{sessionId} {
      allow read, write: if true;
      
      match /teams/{teamId} {
        allow read, write: if true;
      }
    }
  }
}
